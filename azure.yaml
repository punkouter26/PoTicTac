# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoTicTac
metadata:
  template: PoTicTac@0.0.1-beta

# Use Aspire AppHost for infrastructure generation
# Run 'azd infra synth' to generate Bicep from AppHost model
# The AppHost defines all resources (storage, app service, etc.)
services:
  app:
    project: ./src/Po.TicTac.AppHost
    language: dotnet
    host: containerapp
